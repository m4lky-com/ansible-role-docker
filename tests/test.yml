# Playbook to run Docker install role
#
# Example command
# ansible-playbook ansible-role-docker/tests/test.yml

- hosts: localhost
  connection: local
  become: true
  roles:
   - ansible-role-docker